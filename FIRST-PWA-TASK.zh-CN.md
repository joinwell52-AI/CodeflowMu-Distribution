# 手把手完成第一个 ShipReady PWA

这个教程让第一次使用 CodeFlowMu 的人完成一条真实、可验证的交付闭环：从一句需求开始，由 PM、DEV、QA、OPS 协作，最终得到一个可安装、可离线使用、可静态托管的 PWA 小应用。

> [!IMPORTANT]
> 本教程是产品使用示例，不是 Candidate 1 已通过正式 Provider 兼容门禁的声明。开始前必须确认你的 Provider 在当前环境能够正常创建和运行 Agent。仓库公开、静态部署和其他外部写入必须单独获得人的明确批准。

## 你将交付什么

应用名：**ShipReady**

用户可以：

- 新增“今日交付事项”；
- 标记完成或恢复未完成；
- 删除事项并清空已完成事项；
- 查看总数、完成数和完成百分比；
- 刷新页面后保留数据；
- 在手机主屏幕安装应用；
- 首次成功加载后断网继续使用。

预期文件：

```text
shipready/
├─ index.html
├─ styles.css
├─ app.js
├─ manifest.webmanifest
├─ sw.js
├─ icons/
│  ├─ icon-192.png
│  └─ icon-512.png
└─ README.md
```

所有资源必须使用相对路径，确保部署到 GitHub Pages 等仓库子路径时仍能工作。Service Worker 在 `localhost` 或 HTTPS 下运行，不能用双击 `file://` 页面代替 PWA 验证。

## 开始前检查

| 检查 | 成功信号 |
| --- | --- |
| CodeFlowMu 已启动 | 顶部显示连接正常 |
| Provider 已配置 | 模型/Agent 连接验证成功 |
| 示例项目已注册 | 顶部项目名和项目根一致 |
| 项目根不是安装目录 | 业务文件将写入独立项目 |
| 环境预检完成 | 没有阻断任务执行的问题 |
| 项目可回滚 | 空白项目或已有 Git 初始提交 |

如果项目、Runtime、MCP、Watcher 或 Agent 工作目录显示不一致，先停止写入并修复绑定。

## 第一步：创建并选择示例项目

1. 打开 **设置 → 项目**。
2. 创建一个空白项目，或注册一个新的空目录。
3. 将它设为当前项目。
4. 检查顶部显示的项目名称与项目根。
5. 打开 **环境预检**，处理真正的阻断项。

不要把 CodeFlowMu 的 Program Files 安装目录注册为示例项目。

## 第二步：把完整任务交给 PM

复制以下内容：

```text
请开发并交付一个名为 ShipReady 的静态 PWA 小应用。

产品目标：
帮助用户在手机上记录“今天必须完成的交付事项”，并查看完成进度。

功能要求：
1. 用户可以新增一条交付事项。
2. 用户可以把事项标记为完成或未完成。
3. 用户可以删除事项。
4. 用户可以清空全部已完成事项。
5. 页面显示事项总数、已完成数和完成百分比。
6. 数据保存在 localStorage，刷新页面后不能丢失。
7. 页面支持桌面和 390px 手机宽度，不能出现横向滚动。
8. 提供有效的 manifest.webmanifest、Service Worker 和 192/512 PNG 图标。
9. 首次成功加载后，进入离线模式仍可打开和使用。
10. 不使用后端、数据库、CDN、远程字体或外部 API。
11. 所有静态资源使用相对路径，可部署到站点子目录。

工程交付：
- index.html
- styles.css
- app.js
- manifest.webmanifest
- sw.js
- icons/icon-192.png
- icons/icon-512.png
- README.md

验收要求：
- QA 逐条验证新增、完成、恢复、删除、清空、统计和刷新持久化。
- QA 验证 390px 布局、Manifest、Service Worker、离线模式和控制台错误。
- OPS 提供本地静态服务的启动方式、入口 URL 和运行验证。
- 任何 GitHub、Cloudflare 或其他公网部署必须先向我请求明确授权。
- 公网部署获批后，OPS 返回不可含密钥的部署 URL、提交版本和访问证据。
- PM 最后汇总交付文件、验收结果、已知限制和是否建议接受。
```

在 **任务 → 投递审查** 中检查：

- 项目根是否正确；
- 是否限定为静态 PWA；
- 是否禁止外部依赖；
- 是否包含可验证的验收标准；
- 公网部署是否明确要求人工批准。

确认后再发布正式 TASK。

## 第三步：观察 PM 规划和分派

打开任务树。一个合理的计划至少应覆盖：

| 工作 | 建议责任角色 | 必须返回的证据 |
| --- | --- | --- |
| 固定范围和验收标准 | PM | 正式任务与验收清单 |
| 页面、交互和本地存储 | DEV | 变更文件与功能说明 |
| Manifest、Service Worker、图标 | DEV | PWA 文件与注册结果 |
| 本地静态服务 | OPS | 启动命令、URL、健康结果 |
| 功能与持久化验证 | QA | 逐项 PASS/FAIL |
| 手机布局和离线验证 | QA | 视口、离线和控制台证据 |
| 公网静态部署 | OPS | 只在人工批准后执行 |
| 最终复核 | PM | 汇总 REPORT 和接受建议 |

PM 不一定采用完全相同的子任务数量，但不能遗漏验收、运行和证据责任。

## 第四步：检查真实工程产物

在 **文件浏览** 或业务项目目录中确认预期文件已经出现。

重点检查：

- `manifest.webmanifest` 的 `start_url` 和 `scope` 使用适合子路径部署的相对值；
- `index.html` 注册 `sw.js`；
- Service Worker 缓存应用壳，而不是缓存私有数据或凭据；
- 应用不请求远程 CDN、字体、统计脚本或 API；
- `localStorage` 只保存用户输入的本地事项；
- README 说明本地运行、PWA 验证和静态部署方式。

## 第五步：运行和验证

使用 OPS 报告中给出的本地静态服务命令和 URL。不要用 `file://` 验证 Service Worker。

QA 至少应返回：

| 验收项 | 验证动作 | 预期结果 |
| --- | --- | --- |
| 新增 | 连续新增三条事项 | 列表出现三条，统计为 0/3 |
| 完成 | 完成其中两条 | 统计为 2/3，约 67% |
| 恢复 | 恢复一条 | 统计回到 1/3 |
| 删除 | 删除一条 | 总数和完成数同步更新 |
| 清空完成 | 完成后执行清空 | 只删除已完成事项 |
| 持久化 | 刷新页面 | 数据和完成状态保持 |
| 手机布局 | 使用 390px 视口 | 无横向滚动，按钮可点击 |
| Manifest | 浏览器检查 Manifest | 名称、图标、start_url、scope 有效 |
| Service Worker | 检查注册状态 | 激活并控制当前页面 |
| 离线 | 首次加载后切换 Offline 并刷新 | 页面可打开，核心功能可用 |
| 控制台 | 查看 Console | 无未处理错误 |

Browser Use 只有在当前产品实际提供、项目启用并获得授权时才可代替部分人工浏览器操作。工具调用不能代替 QA 的验收映射。

## 第六步：人工决定是否静态发布

本地验收完成后，PM 或 OPS 可以提出公网部署请求，但不能自行发布。

批准前检查：

- 产物只包含这个公开示例应用；
- 没有 `.env`、API Key、绑定链接、日志、TASK、REPORT 或客户数据；
- 没有 CodeFlowMu 产品源码；
- 图标、字体和其他素材具备公开权利；
- 目标仓库、站点和分支准确；
- 静态路径适配站点子目录；
- 你明确理解这是一项外部写入。

可以选择：

- GitHub Pages；
- Cloudflare Pages；
- 其他 HTTPS 静态服务器；
- 暂不公开，只保留本地验收。

OPS 获批后应返回：部署目标、提交版本、公开 URL、HTTP 访问结果、手机访问结果和已知缓存更新方式。

## 第七步：用 CodeFlowMu Mobile PWA 继续第二轮

ShipReady 是团队交付出来的示例应用；CodeFlowMu Mobile PWA 是管理团队的控制入口，两者不是同一个 PWA。

1. 保持 PC 上的 CodeFlowMu 正在运行。
2. 打开 PC 的 **移动端** 页面并刷新绑定信息。
3. 手机通过局域网或公网 Gateway 完成绑定。
4. 在手机上查看 ShipReady 的任务树、REPORT 和实时活动。
5. 从手机向 PM 发送：

```text
请为 ShipReady 增加深色模式。默认跟随系统主题，同时提供手动切换；
刷新后保留用户选择。不要改变已有数据结构。QA 必须回归原有功能、
390px 布局和离线模式；OPS 在原静态地址验证缓存升级。
```

6. 查看第二轮 DEV、QA、OPS 报告。
7. 由人决定接受或继续返工。

不要分享绑定二维码或链接；丢失或停用的手机应从 PC 解除绑定。

## 失败时先看这里

| 现象 | 先检查 |
| --- | --- |
| PM 没有开始 | Provider 连接、模型状态和 Runtime 日志 |
| TASK 写入错误项目 | 顶部项目根、Runtime、MCP、Watcher 与 Agent cwd 是否一致 |
| 页面能打开但 PWA 不工作 | 是否通过 localhost/HTTPS；Manifest 和 Service Worker 路径是否相对 |
| GitHub Pages 首页 404 | 仓库子路径、入口文件位置和 Pages 发布目录 |
| 离线刷新失败 | Service Worker scope、缓存清单和首次在线加载是否完成 |
| 更新后仍看到旧页面 | Service Worker 版本、缓存名称和激活策略 |
| 手机看不到团队状态 | PC 是否运行；Gateway 是否在线；绑定是否过期 |
| OPS 准备直接公开 | 停止执行，先取得人的明确部署授权 |

## 完成标准

第一次任务只有同时满足以下条件才算完成：

- ShipReady 在本地静态服务中通过功能、响应式和离线验收；
- DEV、QA、OPS 都返回了与自身职责匹配的 REPORT 和证据；
- PM 对每条验收标准给出明确结论；
- 人完成最终验收；
- 如果进行了公网部署，部署在明确批准之后发生且公开内容已脱敏；
- CodeFlowMu Mobile PWA 能查看该任务，第二轮修改能够形成新的可追踪交付循环。
